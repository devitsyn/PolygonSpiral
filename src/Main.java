import polygons.DrawWindow;
import polygons.SetupWindow;

public class Main {

    public static void main(String[] args) {
        SetupWindow setupWindow = new SetupWindow();
        setupWindow.setVisible(true);
    }
}
